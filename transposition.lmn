% {op(1*!X+1*!Y+-2*!Z,2).}.

% prepare1@@ {R = I*!X, $p[R] }:- int(I) | {R = mul(I,!X),$p[R]}.
% prepare2@@ {R = A+B, $p[R,A,B]} :- {R = add(A,B),$p[R,A,B]}.

% select(!Y).
// 移項を実現するために，mul が 左辺に繋がっているか右辺に繋がっているか判断したい


% {
%     test(!X).
%     a(X),b(X),c,d,e.
% }.

% {c, $p} :- {f, $p}.
% {a(X), $p[X]} :- {g(X), $p[X]}.

b(X),c(X),a(!X),d,e.